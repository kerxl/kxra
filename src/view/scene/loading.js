import { Scene } from "./scene";

export class LoadingScene extends Scene {
    constructor({ name = "loading", screen }) {
        super({ name: name, screen: screen });
    }

    update(time) {
        
    }
}